<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin Dashboard</title>
  <link rel="stylesheet" href="/css/style.css" />
  <link
    rel="stylesheet"
    href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
  />
</head>
<body>
  <div class="topbar">
    <div class="topbar-brand">
      <button type="button" id="sidebarToggle" class="sidebar-toggle" aria-controls="targetSidebar" aria-expanded="false">
        &#9776;
        <span>Targets</span>
      </button>
      <h1>Live Location Tracker</h1>
    </div>
    <div class="topbar-actions">
      <span id="statusPill" class="status-pill">Waiting for signal</span>
      <a href="/logout">Logout</a>
    </div>
  </div>

  <div class="admin-layout">
    <aside id="targetSidebar" class="target-sidebar">
      <div class="sidebar-header">
        <div>
          <h2>Targets</h2>
          <p>Select a target to open the live map and follow the latest coordinates.</p>
        </div>
        <button type="button" id="sidebarClose" class="sidebar-close" aria-label="Close sidebar">
          &times;
        </button>
      </div>
      <a class="share-link" href="/share" target="_blank" rel="noopener">Open sharing page</a>
      <ul id="targetList" class="target-list">
        <% (targets || []).forEach((target) => { %>
          <li>
            <button type="button" class="target-item" data-target-id="<%= target.id %>">
              <span class="target-id"><%= target.id %></span>
              <span class="target-status" data-target-status>
                <%= target.hasLocation ? "Last update available" : "Waiting for signal" %>
              </span>
            </button>
          </li>
        <% }) %>
      </ul>
      <p id="emptyTargetMessage" class="empty-target-message">Waiting for a user to allow location access.</p>
    </aside>

    <main class="map-panel">
      <div class="map-toolbar">
        <div>
          <span class="eyebrow">Selected target</span>
          <h2 id="selectedTargetLabel">No target selected</h2>
          <div id="selectedTargetMeta" class="selected-meta">Click a target to open the live map.</div>
        </div>
      </div>
      <div id="map"></div>
    </main>
  </div>

  <div id="sidebarBackdrop" class="sidebar-backdrop" hidden></div>

  <script>
    window.ADMIN_TARGETS =  <%- JSON.stringify(targets || []) %>;
  </script>
  <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
  <script src="/socket.io/socket.io.js"></script>
  <script src="/javascripts/admin.js"></script>
</body>
</html>
